.Top-menu
  .Title
    = link_to root_path do
      %spanなんでもランキング
  .Head-items
    -if user_signed_in?
      ユーザー名:
      = current_user.name
      = link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'Head-items__logout-btn'
      = link_to "ユーザー編集", edit_user_registration_path(current_user), class: "Head-items__edit-btn"
    - else
      = link_to "ログイン", new_user_session_path, class: 'Head-items__logout-btn'
      = link_to "新規会員登録", new_user_registration_path, class: 'Head-items__logout-btn'
